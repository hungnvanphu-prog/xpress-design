# Basic HTTP Auth — tắt mặc định. Bật: bỏ # 2 dòng dưới, tạo infra/nginx/auth/.htpasswd
# (xem ../README-htauth.md), rồi: docker compose restart nginx
#
auth_basic "Xpress (preview)";
auth_basic_user_file /etc/nginx/auth/.htpasswd;
