DATABASE_URL=postgresql://admin:admin@postgres:5432/app_db
JWT_SECRET=change-me
JWT_EXPIRES_IN=7d
PORT=4000

# Production: bắt buộc — danh sách origin (phẩy), ví dụ: https://example.com,https://www.example.com
# Local dev: có thể bỏ trống (CORS tạm mở); production không có sẽ lỗi khi start.
# ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# CMS proxy (optional)
STRAPI_URL=http://cms:1337
STRAPI_TOKEN=
